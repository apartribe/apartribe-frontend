export interface AptSearch {
  name: string
  id: string
  address: string
}

export interface AptInfo {
  id: number
  img: string
  badge: string
  name: string
  location: string
  recentPost: string
  userCount: number
}
