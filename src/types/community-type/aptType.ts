export interface AptSearch {
  name: string
  id: string
  address: string
}

export interface AptInfo {
  id: string
  img: string
  badge: string
  name: string
  location: string
  recentPost: string
  userCount: number
}
